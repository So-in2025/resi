🌐 Proyecto: Centro de Resiliencia Económica Argentina (Resi)
Última Revisión de Arquitectura: 9 de Septiembre de 2025

Índice del Documento:
1. Objetivo General
2. Arquitectura Técnica Detallada
   2.1. Backend (FastAPI)
   2.2. Frontend (Next.js & TypeScript)
3. Flujo de Datos Crítico: Ejemplo de Chat con la IA
4. Esquema de la Base de Datos (Final)
5. Roadmap de Módulos (Actuales y Futuros)
6. Historial de Actualizaciones Recientes

---

### 1. Objetivo General
Crear un espacio digital gratuito, interactivo y visual, que empodere al usuario promedio argentino a mejorar su economía, producir alimentos sostenibles, ahorrar inteligentemente y planificar la vida familiar, integrando IA práctica y personalizada, gamificación y comunidad.

---

### 2. Arquitectura Técnica Detallada
La plataforma está construida sobre una arquitectura desacoplada con un frontend moderno y un backend modular, preparada para escalar.

#### 2.1. Backend (FastAPI)
La API está diseñada para ser robusta, escalable y fácil de mantener.

* **main.py: Orquestador Principal**
    * **Función:** Inicializa FastAPI, configura middlewares (CORS), incluye los routers de los módulos y define endpoints globales como `/chat` y `/onboarding-complete`.
    * **Inteligencia Central:** Contiene el "prompt de sistema" que define la personalidad y las reglas de la IA de Resi. Reúne todo el contexto del usuario (financiero, perfil, historial) antes de cada llamada a la IA.

* **database.py: Definición de la Estructura de Datos**
    * **Función:** Define el esquema de la base de datos usando SQLAlchemy ORM. Modela todas las tablas: `User`, `Expense`, `BudgetItem`, `SavingGoal`, `ChatMessage`, y `FamilyPlan`.

* **routers/: Lógica de Negocio Modular**
    * **finance.py:** Maneja toda la lógica financiera: presupuestos, gastos, metas y análisis.
    * **family.py:** Gestiona la creación y recuperación de planes familiares personalizados.
    * **cultivation.py:** Contiene la lógica para los planes de cultivo.

* **dependencies.py: Funciones Auxiliares**
    * **Función:** Provee dependencias reutilizables, como la obtención del usuario actual (`get_user_or_create`) y el análisis de texto para gastos (`parse_expense_with_gemini`).

* **schemas.py: Validación de Datos**
    * **Función:** Define los modelos de Pydantic que aseguran que los datos que entran y salen de la API tengan la estructura correcta.

#### 2.2. Frontend (Next.js & TypeScript)
La interfaz de usuario es una Single Page Application (SPA) construida para ser rápida, reactiva y adaptable a cualquier dispositivo.

* **`src/app/page.tsx`:** Es el componente principal que renderiza la página de inicio y organiza todos los módulos dentro de acordeones. Gestiona el estado global de la interfaz, como qué módulo está abierto y la visibilidad del chat.

* **`src/components/`: Componentes Reutilizables**
    * **OnboardingFlow.tsx:** Guía a los nuevos usuarios a través de una serie de pasos para recolectar información esencial (ingresos, perfil de riesgo, metas), personalizando la experiencia desde el inicio.
    * **FamilyPlannerModule.tsx:** Componente interactivo que permite a los usuarios definir las características de su familia y recibir un plan de comidas, ahorro y ocio generado por la IA. Guarda y recupera el último plan generado.
    * **ChatWindow.tsx:** Interfaz del chat flotante que muestra la conversación y permite al usuario interactuar por texto o voz (usando el hook `useResiVoice`).
    * **FinanceModule.tsx, CultivationModule.tsx, etc.:** Componentes dedicados que encapsulan la lógica y la UI de cada módulo principal.

* **`src/hooks/useResiVoice.ts`:** Hook personalizado que encapsula la lógica del reconocimiento de voz del navegador (`SpeechRecognition`), convirtiendo la voz del usuario en texto para ser enviado al chat.

* **`src/lib/apiClient.ts`:** Una instancia configurada de Axios que centraliza todas las llamadas a la API del backend, manejando la inyección de la URL base desde las variables de entorno.

---

### 3. Flujo de Datos Crítico: Ejemplo de Chat con la IA

1.  **Usuario (Frontend):** El usuario escribe o dicta una pregunta en el `ChatWindow.tsx`.
2.  **Llamada a la API (Frontend):** El componente `page.tsx` llama al endpoint `POST /chat` del backend, enviando la pregunta.
3.  **Recopilación de Contexto (Backend):** La función `ai_chat` en `main.py`:
    a. Guarda la pregunta del usuario en la tabla `ChatMessage`.
    b. Consulta la base de datos para obtener el resumen financiero, el perfil de riesgo, las metas a largo plazo y el último plan familiar del usuario.
    c. Recupera los últimos 10 mensajes del historial de chat.
    d. Ensambla toda esta información en un contexto consolidado.
4.  **Consulta a la IA (Backend):** El contexto completo y la nueva pregunta se envían al modelo de IA (Google Gemini).
5.  **Respuesta de la IA (Backend):** La IA genera una respuesta en texto plano, siguiendo las reglas de su prompt de sistema.
6.  **Guardado y Envío (Backend):** El backend guarda la respuesta de la IA en la tabla `ChatMessage` y la envía de vuelta al frontend.
7.  **Renderizado (Frontend):** El `ChatWindow.tsx` recibe la respuesta y la muestra como un nuevo mensaje de Resi.

---

### 4. Esquema de la Base de Datos (Final)

* **users**
    * `email` (PK, String)
    * `has_completed_onboarding` (Boolean)
    * `risk_profile` (String, Nullable)
    * `long_term_goals` (Text, Nullable)

* **expenses**
    * `id` (PK, Integer), `description` (String), `amount` (Float), `category` (String), `date` (DateTime), `user_email` (FK)

* **budget_items**
    * `id` (PK, Integer), `category` (String), `allocated_amount` (Float), `is_custom` (Boolean), `user_email` (FK)

* **saving_goals**
    * `id` (PK, Integer), `name` (String), `target_amount` (Float), `current_amount` (Float), `user_email` (FK)

* **chat_messages**
    * `id` (PK, Integer), `sender` (String), `message` (String), `timestamp` (DateTime), `user_email` (FK)

* **family_plans**
    * `id` (PK, Integer), `meal_plan_json` (Text), `budget_suggestion` (Text), `leisure_suggestion_json` (Text), `created_at` (DateTime), `user_email` (FK)

---

### 5. Roadmap de Módulos

* **Módulo 1: Finanzas (✅ Implementado)**
* **Módulo 2: Cultivo (✅ Implementado)**
* **Módulo 3: Planificación Familiar (✅ Implementado y Mejorado)**
* **Módulo 4: Chat con IA (✅ Implementado con Hiper-Personalización)**
* **Módulo 5: Comunidad y Gamificación (Pendiente)**
* **Módulo 6: Mercado Resiliente (Pendiente)**
* **Módulo 7: Educación Financiera (Pendiente)**
* **Módulo 8: Energías Renovables (Pendiente)**
* **Módulo 9: Suscripción Premium (Pendiente)**

---

### 6. Historial de Actualizaciones Recientes

* **Septiembre 2025 - "Hiper-Personalización" (Ruta 1):**
    * Se amplió el flujo de Onboarding para incluir **Perfil de Riesgo** y **Metas a Largo Plazo**.
    * Se expandió el **Planificador Familiar** con más opciones y se implementó el guardado y carga de planes.
    * Se reestructuró la **base de datos** para almacenar esta nueva información contextual.
    * Se actualizó el **cerebro de la IA (`main.py`)** para que utilice todo el contexto del usuario (finanzas, perfil, metas, plan familiar, historial de chat) antes de generar cada respuesta, logrando un nivel de personalización significativamente mayor.
    * Se resolvieron múltiples errores de frontend, incluyendo el **`ERR_NETWORK`** por desincronización de la base de datos y se corrigieron problemas de CSS en el sidebar y módulos.